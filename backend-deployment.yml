apiVersion: apps/v1
kind: Deployment
metadata:
  name: neostreamapiserver
spec:
  selector:
    matchLabels:
      app: neostreamapi
  template:
    metadata:
      labels:
        app: neostreamapi
    spec:
      containers:
      - name: neostreamapi
        image: backend-neostream:v4.0
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 4000
        env: 
        - name: MONGODB_URL
          valueFrom: 
            secretKeyRef:
              name: dburl
              key: mongo_url
        - name: REDIS_SERVER
          value: "localhost"
      - name: redis
        image: redis
        resources: 
          limits: 
            memory: "64Mi"
            cpu: "100m"
        ports:
        - containerPort: 6379



